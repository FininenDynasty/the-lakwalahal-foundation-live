{
  "version": 2,
  "name": "lakwalahal-foundation",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "build",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}